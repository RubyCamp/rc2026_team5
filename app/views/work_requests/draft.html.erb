<<<<<<< HEAD
<!-- GETのレスポンスとして、showページ内の割当欄だけを差し替える。 -->
<turbo-frame id="work-request-assignments">
  <%# 仮割当直後の最新状態をDBから読み直してshow側へ返す。 %>
  <% assignments = @work_request.assignments.reload %>
  <section class="card shadow-sm">
    <div class="card-body">
      <h2 class="h4 card-title">現在の割当</h2>
      <% if assignments.any? %>
        <ul class="list-group list-group-flush">
          <% assignments.each do |assignment| %>
            <li class="list-group-item px-0 d-flex justify-content-between align-items-center gap-3">
              <strong><%= assignment.staff_member.name %></strong>
              <span class="badge text-bg-secondary"><%= enum_label(assignment, :status) %></span>
            </li>
          <% end %>
        </ul>
      <% else %>
        <p class="mb-0 text-body-secondary">まだスタッフは割り当てられていません。</p>
      <% end %>
    </div>
  </section>
</turbo-frame>
=======
<h1>仮割当が完了しました．</h1>
<div class="gap-2 mb-4 text-end">
<%= button_to "戻る", {controller: 'work_requests', action: 'index'}, {method: :get, class:'btn btn-primary ' } %>
</div>
>>>>>>> d7d38ec (スタッフ一覧の表示を直した)
